# 定时任务类 - 自定义扩展检查项

> 本文件用于扩展 req-check-scheduled-task 的检查点。
> 在此追加的检查项会在每次定时任务类需求评审时自动纳入检查范围。
>
> **如何新增检查项：**
> 在对应分类下按以下格式追加一行：
> `| 检查项名称 | 检查内容描述 | 适用场景（可选） |`
> 保存后，下次运行 /req-check-scheduled-task 或 /req-review-agent 时自动生效。

---

## 扩展检查项

> 当前暂无自定义扩展检查项，可按上方说明追加。

<!-- 示例（取消注释即可启用）：
| 依赖服务检查 | 任务执行前是否需要检查依赖服务可用性，不可用时的处理逻辑 | 有外部依赖的任务 |
| 执行窗口保护 | 是否需要在特定时间段内禁止执行（如账期结算期间） | 与账期相关的任务 |
-->
