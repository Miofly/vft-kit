# data-model.md 模板

```markdown
# 数据模型

## 核心实体

### {实体名}

| 字段 | 类型 | 说明 |
|------|------|------|
| {field} | {type} | {描述} |

## 实体关系

```mermaid
erDiagram
    {实体A} ||--o{ {实体B} : "{关系描述}"
    {实体B} }o--|| {实体C} : "{关系描述}"
```

> 使用 Mermaid erDiagram 语法。关系符号: `||` 一对一必需, `o{` 零或多, `|{` 一或多, `o|` 零或一。根据实际实体关系绘制。

## 数据库 Schema

{如适用，列出关键表/集合的结构}

## 数据验证规则

| 实体 | 字段 | 规则 |
|------|------|------|
| {entity} | {field} | {validation} |
```
