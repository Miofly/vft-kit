# api-surface.md 模板

```markdown
# API 接口

## 概览

{API 类型: REST / GraphQL / gRPC / CLI}
{基础 URL / 端口}
{认证方式}
路由文件数: {N}，分析模式: {全量/摘要/采样}

## 端点列表

### {分组名（按模块或功能域分组）}

| 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|
| {GET/POST/...} | {path} | {说明} | {是/否} |

{采样模式下注明: > 该模块共 X 个端点，此处列出 Y 个代表性端点}

### {端点详情 — 仅全量模式(N ≤ 15)展开此段}

**{METHOD} {path}**

参数:
| 参数名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| {name} | {type} | {是/否} | {描述} |

返回:
{返回值结构}

## 内部接口契约

{模块间的主要接口约定 — 仅列出关键的跨模块调用}
```
